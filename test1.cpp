#include<bits/stdc++.h>

using namespace std;


int main()

{
    int m=10;
    // string ss = '9';
    // cin>>m;
    // char buffp[100] = '9';
    // int err,fd[2];
    // err= pipe(fd);
    // close(fd[0]);
    // write(fd[1],buffp,strlen(buffp));
    // cout<<"abc has written in pipe\n";
    // close(fd[1])
    cout<<m<<endl;
}